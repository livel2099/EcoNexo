{
  "name": "econexo-web",
  "version": "1.0.0-rc.6.2",
  "private": true,
  "scripts": {
    "dev": "next dev -H 0.0.0.0",
    "dev:demo": "cross-env NEXT_PUBLIC_DEMO_MODE=true next dev -H 0.0.0.0",
    "build": "next build",
    "build:cloudflare": "cross-env NEXT_PUBLIC_DEMO_MODE=true next build",
    "types:cloudflare": "wrangler types --include-runtime false",
    "check:cloudflare": "npm run build:cloudflare && wrangler types --include-runtime false --check",
    "preview:cloudflare": "npm run build:cloudflare && wrangler dev",
    "deploy:cloudflare:dry-run": "npm run build:cloudflare && wrangler deploy --dry-run",
    "deploy:cloudflare": "npm run build:cloudflare && wrangler deploy",
    "start": "next start -H 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "validate": "npm run typecheck && npm run build",
    "build:cloudflare:production": "cross-env NEXT_PUBLIC_DEMO_MODE=false NEXT_PUBLIC_STATIC_EXPORT=true next build",
    "preview:cloudflare:production": "npm run build:cloudflare:production && wrangler --config wrangler.production.jsonc dev",
    "deploy:cloudflare:production:dry-run": "npm run build:cloudflare:production && wrangler --config wrangler.production.jsonc deploy --dry-run",
    "deploy:cloudflare:production": "npm run build:cloudflare:production && wrangler --config wrangler.production.jsonc deploy"
  },
  "dependencies": {
    "leaflet": "1.9.4",
    "next": "15.5.23",
    "react": "19.0.8",
    "react-dom": "19.0.8"
  },
  "devDependencies": {
    "@types/leaflet": "1.9.15",
    "@types/node": "22.10.2",
    "@types/react": "19.0.2",
    "cross-env": "^10.1.0",
    "typescript": "5.7.2",
    "wrangler": "^4.123.0"
  },
  "overrides": {
    "postcss": "8.5.26",
    "sharp": "0.35.3",
    "nanoid": "3.3.18"
  },
  "engines": {
    "node": ">=22.16.0 <23.0.0"
  }
}
